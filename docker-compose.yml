services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - BASE_URL=http://localhost:8080
      - REDIS_ADDR=redis:6379
    depends_on:
      - redis
      - REDIS_TTL=24h    # 正式環境
